## 1. Зависимость Ax

- [x] 1.1 Добавить Python-пакет `axllm` в `pyproject.toml` и обновить lockfile через `uv add axllm`; проверить, что `uv sync --extra dev` проходит и `from axllm import ax, OpenAICompatibleClient` импортируется
- [x] 1.2 Снять прямой импорт `openai` из прикладного кода (`app/`) и убрать `openai` из прямых зависимостей проекта, если пакет больше не импортируется приложением; проверить `rg "from openai|import openai" app` без совпадений

## 2. Программа и провайдер

- [x] 2.1 Собрать `OpenAICompatibleClient` из `LMSTUDIO_BASE_URL`, `LMSTUDIO_API_KEY`, `LMSTUDIO_MODEL`, температуры `0.2` и таймаута `LMSTUDIO_TIMEOUT_SECONDS`; проверить тестом, что запрос генерации уходит на настроенный локальный URL с этим `model` и не на `api.openai.com`
- [x] 2.2 Описать сигнатуру чата (`ax()`): вход — история `user`/`assistant` из запроса, выход — текст ответа; инструкцию брать из `prompt.py` (поддержка врача, не диагноз, без отделения); проверить тестом, что многоходовая история попадает во вход и клиентский `role: system` по-прежнему даёт HTTP 400/422
- [x] 2.3 Перевести `LLMClient.complete` на `program.forward` (синхронный Ax — через `asyncio.to_thread`, если нет async API) и сохранить маппинг ошибок 503/504/502; проверить тестами мока: успешный непустой ответ и пустой/невалидный выход → HTTP 502 `llm_error`
- [x] 2.4 Перевести `start_chat_stream` на стрим Ax и отдавать в SSE только инкременты текста ответа с прежним терминальным событием; проверить HTTP-тестом `stream: true` — фрагменты + done, контракт `text/event-stream` без изменений
- [x] 2.5 Реализовать `ping` / `GET /ready` без прогона программы чата (HTTP `GET {base}/models` или эквивалент провайдера); проверить: апстрим жив → 200 `ready`, отказ соединения → 503 `not_ready`

## 3. Регрессия API

- [x] 3.1 Прогнать существующие HTTP-тесты чата (нестрим, disclaimer, недоступный апстрим, SSE при недоступной модели) и поправить только моки LLM, не контракт роутера; проверить, что `make test` зелёный
- [x] 3.2 Убедиться, что в коде нет GEPA/agent/MCP/tools на пути чата и что генерация не требует артефакта оптимизатора; проверить тестом или смоуком: чат проходит с моком без файлов оптимизации

## 4. Документация

- [x] 4.1 Обновить README: зависимость `axllm`, что чат идёт через сигнатуру Ax на тот же LM Studio, облачные ключи OpenAI не нужны; проверить, что примеры `curl` из README по-прежнему валидны без правок путей и полей JSON
