## 1. Совместимость исходящего запроса

- [x] 1.1 Заменить объявление `structuredOutputModes` с `json_object` на `text` в `app/llm.py` и проверить перехватом транспорта, что `response_format` типа `json_object` больше не уходит
- [x] 1.2 Добавить нормализацию сообщений: подряд идущие сообщения одной роли объединяются в одно; проверить, что в исходящем запросе роли чередуются
- [x] 1.3 Проверить на живой LM Studio, что запрос перестал отклоняться с `Conversation roles must alternate`

## 2. Принуждение структуры ответа

- [x] 2.1 Построить схему ответа по выходным полям сигнатуры программы и подставлять её в исходящий запрос как `response_format` типа `json_schema`; проверить, что схема соответствует полям сигнатуры
- [x] 2.2 Проверить на живой MedGemma, что нестриминговый `POST /api/v1/chat` возвращает 200 и осмысленный текст ответа, а не рассуждение модели
- [x] 2.3 Проверить на живой MedGemma, что потоковый `POST /api/v1/chat` со `stream: true` отдаёт SSE-кадры с текстом и завершается `[DONE]`

## 3. Проверка совместимости и отладочное рассуждение

- [x] 3.1 Реализовать проверку совместимости при старте: доступная LM Studio с моделью, не принимающей схему, останавливает процесс с внятным сообщением; недоступная LM Studio запуску не мешает — проверить оба случая
- [x] 3.2 Включить ту же проверку в `GET /ready` и проверить, что подменённая после старта несовместимая модель даёт `not_ready` с машиночитаемым кодом
- [x] 3.3 Добавить настройку включения отладочного лога рассуждения (по умолчанию выключена) и записывать `reasoning_content` отдельным логгером, не `app.access`; проверить, что при выключенной настройке рассуждение не пишется
- [x] 3.4 Проверить, что рассуждение модели не попадает в ответ API ни при включённой, ни при выключенной настройке
- [x] 3.5 Проверить, что существующий тест `test_access_log_omits_chat_body` продолжает проходить при включённой настройке

## 4. Тесты и документация

- [x] 4.1 Добавить в `tests/test_llm.py` проверку формы исходящего запроса: роли чередуются, присутствует `response_format` типа `json_schema`, `json_object` не встречается; проверить `make test`
- [x] 4.2 Добавить тест на ошибку разбора ответа: неразобранный ответ модели не выдаётся как текст ассистента, возвращается код ошибки
- [x] 4.3 Дополнить `README.md` требованием, что загруженная в LM Studio модель должна поддерживать structured output по схеме
- [x] 4.4 Задокументировать в `README.md` переменную включения отладочного лога рассуждения с предупреждением, что рассуждение содержит клинический текст запроса
