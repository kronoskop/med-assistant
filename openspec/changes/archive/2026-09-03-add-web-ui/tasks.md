## 1. Отдача интерфейса

- [x] 1.1 Добавить `app/web.py` с явными роутами `/ui`, `/ui/app.js`, `/ui/styles.css` (без `mount`) и проверить, что `GET /ui` отдаёт 200 с `text/html`
- [x] 1.2 Подключить роутер в `app/main.py` и проверить, что `GET /health` по-прежнему JSON, а `GET /no-such-route` — 404 с `{code, message}`
- [x] 1.3 Проверить, что `GET /ui/no-such-file` возвращает 404 в JSON-формате, а не текстовую ошибку статики

## 2. Перенос макета

- [x] 2.1 Перенести палитру, типографику и метрики артборда в `app/static/styles.css`, вынести значения плотности в кастомные свойства и проверить переключатель «Плотно / Свободно» в браузере
- [x] 2.2 Собрать каркас экрана (боковая панель, шапка, лента, композер) в `app/static/index.html` и сверить с артбордом при ширине 1440×900
- [x] 2.3 Заменить иконочный шрифт Phosphor на встроенные SVG и проверить отображение значков при заблокированной внешней сети
- [x] 2.4 Подключить шрифты PT Serif и Manrope с полными запасными стеками и проверить, что без сети вёрстка не разрушается
- [x] 2.5 Перенести словари ru/uz/en и переключатель языка, проверить все три языка на полном экране

## 3. Живой диалог

- [x] 3.1 Реализовать отправку `POST /api/v1/chat` со `stream: true`, разбор SSE и дописывание текста в ленту; проверить на живом LM Studio, что текст появляется по частям
- [x] 3.2 Реализовать остановку потока через `AbortController` и проверить, что полученная часть остаётся в ленте и помечена как прерванная
- [x] 3.3 Реализовать клиентское владение историей: список `messages` во вкладке, целиком уходит в каждый запрос; проверить, что второй вопрос отправляется вместе с предыдущими сообщениями
- [x] 3.4 Проверить, что после перезагрузки страницы лента пуста и содержимое прошлых диалогов недоступно
- [x] 3.5 Реализовать «Переспросить» через повтор последнего вопроса с усечённой историей и проверить, что прошлый ответ заменяется, а не дублируется
- [x] 3.6 Реализовать опрос `GET /ready` и три состояния индикатора; проверить с выключенным LM Studio, что показывается недоступность
- [x] 3.7 Обработать обрыв потока без `[DONE]` как сбой: показать сообщение, пометить ответ неполным, предложить повтор; проверить, прервав соединение в середине генерации
- [x] 3.8 Отобразить коды `llm_unavailable`, `llm_timeout`, `llm_error`, `validation_error` человекочитаемо с сохранением кода; проверить каждый, подменяя ответ сервера

## 4. Курируемые темы

- [x] 4.1 Перенести шесть тем макета как карточки и чипы, проверить, что нажатие отправляет вопрос в модель
- [x] 4.2 Собрать карту «тема → документы» и показывать панель руководств с подписью «где сверить»; проверить, что поабзацные сноски нигде не появляются
- [x] 4.3 Собрать карту «тема → признаки тревоги» из курируемых списков и показывать блок только при сопоставлении с темой; проверить, что на свободном вопросе блок отсутствует
- [x] 4.4 Подписать блок признаков тревоги протоколом-источником и проверить, что подпись не приписывает содержимое модели
- [x] 4.5 Действующей считается последняя редакция, опубликованная на сайте центра: номер в интерфейс не зашивается, подпись под признаками тревоги ведёт в раздел «Протоколы и стандарты»; проверено на всех трёх языках

## 5. Разделение локального контента и вывода модели

- [x] 5.1 Перенести карточки «Где сверить» и «Консультация центра» как локальный контент и пометить их видимой подписью; проверить, что подпись видна в ленте
- [x] 5.2 Исключить локальные карточки из отправляемого списка `messages` и проверить в сетевой панели, что их текст не уходит в запрос
- [x] 5.3 Вывести предупреждение о недопустимости ввода персональных данных пациента и проверить, что оно видно на всех трёх языках

## 6. Тесты и документация

- [x] 6.1 Добавить `tests/test_web.py`: интерфейс отдаётся, тип содержимого HTML, статика доступна; проверить `make test`
- [x] 6.2 Перепривязать сценарий «тело ответа не является HTML» в `tests/test_http.py` к путям API и проверить, что весь набор тестов проходит
- [x] 6.3 Дополнить `README.md` адресом интерфейса и предупреждением об отсутствии аутентификации; проверить, что описанный сценарий запуска воспроизводится с нуля
